services:
  ubuntu-desktop-lxde-vnc:
    image: dorowu/ubuntu-desktop-lxde-vnc:latest
    container_name: ubuntu-desktop-lxde-vnc
    restart: unless-stopped

    labels:
      - autoheal-app=true
      - keep_healthy
      - "com.centurylinklabs.watchtower.enable=true"

    environment:
      - "ALSADEV=hw:1,0"
      - "USER=${UBUNTU_USER:-ubuntu}"
      - "PASSWORD=${UBUNTU_PASSWORD:-ubuntu}"
      - "VNC_PASSWORD=${UBUNTU_VNC_PASSWORD:-ubuntu}"

    volumes:
      - "/dev/shm:/dev/shm"

    hostname: ubuntu-desktop-lxde-vnc

    networks:
      - management-services
      - host

    ports:
      - "6080:80"
      - "5900:5900"

    devices:
      - "/dev/snd"

    env_file:
      - .env.ubuntu-desktop-lxde-vnc

    deploy:
      replicas: 1
      # resources:
      #   limits:
      #     memory: 512M
      #     cpus: "0.50"
      #   reservations:
      #     memory: 256M
      #     cpus: "0.25"
