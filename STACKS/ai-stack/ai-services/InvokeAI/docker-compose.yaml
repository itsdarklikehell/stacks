services:
  InvokeAI:
    image: ${IMAGE:-ghcr.io/invoke-ai/invokeai:latest}
    container_name: ${NAME:-InvokeAI}
    restart: ${RESTART:-unless-stopped}

    labels:
      - com.centurylinklabs.watchtower.enable=true
      - com.ouroboros.enable=true
      - autoheal-app
      - autoheal-app=true
      - homarr-app=true
      - homarr

    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-Europe/Amsterdam}
      - NVIDIA_VISIBLE_DEVICES=all
      - ALLOW_UNKNOWN_MODELS=true
      - INVOKEAI_ALLOW_UNKNOWN_MODELS=true
      # - SCAN_MODELS_ON_STARTUP=true
      # - INVOKEAI_SCAN_MODELS_ON_STARTUP=true
      - INVOKEAI_ENABLE_PARTIAL_LOADING=true
      - ENABLE_PARTIAL_LOADING=true
      # - DEVICE_WORKING_MEM_GB=4
      # - INVOKEAI_DEVICE_WORKING_MEM_GB=4
      # - INVOKEAI_KEEP_RAM_COPY_OF_WEIGHTS=false
      # - KEEP_RAM_COPY_OF_WEIGHTS=false

    volumes:
      # - /etc/localtime:/etc/localtime:ro
      # - /etc/timezone:/etc/timezone:ro
      - /var/run/docker.sock:/var/run/docker.sock

      - InvokeAI_data:/invokeai

      # MODELS:
      - InvokeAI_models:/invokeai/models

      # - anything-llm_models:/invokeai/models/anything-llm_models
      # - comfyui_models:/invokeai/models/comfyui_models
      # - forge_models:/invokeai/models/forge_models
      # - localai_models:/invokeai/models/localai_models
      # - ollama_models:/invokeai/models/ollama_models

      # OUTPUTS
      - anything-llm_output:/invokeai/outputs/anything-llm_outputs
      - comfyui_output:/invokeai/outputs/comfyui_outputs
      - forge_output:/invokeai/outputs/forge_outputs
      - InvokeAI_output:/invokeai/outputs
      - localai_output:/invokeai/outputs/localai_outputs

      # # INPUTS
      # - anything-llm_input:invokeai/inputs/anything-llm_input
      # - comfyui_input:invokeai/inputs/comfyui_inputs
      # - forge_input:invokeai/inputs/forge_input
      # - InvokeAI_input:invokeai/inputs/InvokeAI_input
      # - localai_input:invokeai/inputs

    hostname: ${NAME:-InvokeAI}

    # user: ${PUID:-1000}:${PGID:-1000}

    networks:
      host:
      ai-services:

    ports:
      - ${PORT:-9090}:9090

    env_file:
      - ./InvokeAI/.env

    stop_signal: SIGKILL

    devices:
      - /dev/nvidia0:/dev/nvidia0
      - /dev/dri:/dev/dri

    runtime: nvidia
    deploy:
      replicas: 1
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, compute, video, graphics, utility]

    tty: true

    stdin_open: true

    # healthcheck:
    # test:
    #   [
    #     CMD,
    #     wget,
    #     --no-verbose,
    #     --tries=3,
    #     --spider,
    #     http://localhost:${PORT:-9090}/api/health,
    #   ]
    #   interval: 30s
    #   timeout: 10s
    #   start_period: 30s
    #   retries: 3

    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 3
