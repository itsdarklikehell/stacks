services:
  webcord:
    image: lscr.io/linuxserver/webcord:latest
    container_name: webcord

    volumes:
      - webcord_config:/config

    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-Europe/Amsterdam}

    env_file:
      - ./webcord/.env

    ports:
      - ${PORT:-3188}:3000
      - 3938:3001

    shm_size: 1gb

    networks:
      host:
      chat-services:

    restart: unless-stopped
