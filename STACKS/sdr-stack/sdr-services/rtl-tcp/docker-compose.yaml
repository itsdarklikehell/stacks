services:
  rtl-tcp:
    image: ${IMAGE:-gghcr.io/lizenzfass78851/docker-rtl-tcp:latest}
    container_name: ${NAME:-rtl-tcp}
    restart: ${RESTART:-unless-stopped}

    build:
      context: ../../../DATA/sdr-stack/rtl-tcp
      dockerfile: Dockerfile
      args:
        UID: ${PUID:-1000}
        GID: ${PGID:-1000}

    labels:
      - com.centurylinklabs.watchtower.enable=true
      - com.ouroboros.enable=true
      - autoheal-app
      - autoheal-app=true
      - homarr-app=true
      - homarr

    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-Europe/Amsterdam}

      - OAUTHLIB_RELAX_TOKEN_SCOPE=1
      - DOCKER_MODS=linuxserver/mods:universal-package-install|linuxserver/mods:universal-git|linuxserver/mods:universal-cron|lscr.io/linuxserver/mods:universal-unrar6
      # - DOCKER_HOST=
      - INSTALL_PACKAGES=libfuse2|git|gdb
      - RESTART_APP=true

      - rtl-tcp_FREQS=${rtl-tcp_FREQS:-123.9}

    volumes:
      - /etc/localtime:/etc/localtime:ro
      # - /etc/timezone:/etc/timezone:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - /dev/bus/usb:/dev/bus/usb:ro

      - rtl-tcp_config:/run/rtlsdir-airband

    tmpfs:
      - /tmp:mode=1777

    hostname: ${NAME:-rtl-tcp}

    # user: ${PUID:-1000}:${PGID:-1000}

    networks:
      host:
      sdr-services:

    ports:
      - ${PORT:-1234}:1234

    env_file:
      - ./rtl-tcp/.env

    healthcheck:
      disable: true

    device_cgroup_rules:
      - c 189:* rwm

    runtime: nvidia
    deploy:
      replicas: 1
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu, compute, video, graphics, utility ]

    tty: true

    stdin_open: true

    # shm_size: 1gb

    # healthcheck:
    #   test:
    #     [
    #       CMD,
    #       wget,
    #       --no-verbose,
    #       --tries=3,
    #       --spider,
    #       http://localhost:${PORT:-12393}/api/health,
    #     ]
    #   interval: 30s
    #   timeout: 10s
    #   start_period: 30s
    #   retries: 3

    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 3

