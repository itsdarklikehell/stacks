services:
  flemmarr:
    container_name: ${NAME:-flemmarr}
    image: ${IMAGE:-pierremesure/flemmarr}
    restart: ${RESTART:-unless-stopped}

    labels:
      - autoheal-app=true
      - autoheal
      - "com.centurylinklabs.watchtower.enable=true"

    volumes:
      - ../DATA/flemmarr/config:/config

    hostname: ${NAME:-flemmarr}

    # command: [ "ollama", "pull", "qwen3"]

    networks:
      host:
      arr-services:

    # # ports:
    # #   - "80:80"

    env_file:
      - ./flemmarr/.env

    deploy:
      replicas: 1
      # resources:
      #   limits:
      #     memory: 512M
      #     cpus: "0.50"
      #   reservations:
      #     memory: 256M
      #     cpus: "0.25"

    # depends_on:
    #   - faster-whisper-gpu
    #   - wyoming-piper
