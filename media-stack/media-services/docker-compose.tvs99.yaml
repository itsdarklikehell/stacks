services:
  tvs99:
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.25"
        reservations:
          memory: 128M
          cpus: "0.10"
    image: "zshall/television-simulator:latest"
    env_file:
      - ../.env
    container_name: tvs99
    hostname: tvs99
    restart: unless-stopped
    networks:
      - media-services
    ports:
      - ${TVS99_PORT:-8085}:3000
    volumes:
      - ../DATA/tvs99/config:/config
      - ../DATA/tvs99/data:/data
    labels:
      autoheal: true
      keep_healthy:

networks:
  ai-stack:
    name: ai-stack
