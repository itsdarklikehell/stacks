services:
  flexget:
    image: ${IMAGE:-flexget/flexget}
    container_name: ${NAME:-flexget}
    restart: ${RESTART:-unless-stopped}

    labels:
      - autoheal-app=true
      - autoheal
      - "com.centurylinklabs.watchtower.enable=true"

    environment:
      - TZ=${TZ:-Europe/Amsterdam}

    volumes:
      - ../DATA/flexget/config:/config
      - ../DATA/flexget/downloads:/downloads

    hostname: ${NAME:-flexget}

    networks:
      host:
      media-services:

    ports:
      - "${PORT:-5050}:5050"

    env_file:
      - ./flexget/.env

    deploy:
      replicas: 1
      # resources:
      #   limits:
      #     memory: 512M
      #     cpus: "0.50"
      #   reservations:
      #     memory: 256M
      #     cpus: "0.25"

    command:
      - daemon
      - start
      - --autoreload-config




    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
