services:
  plex:
    image: ${PLEX_IMAGE:-plexinc/pms-docker:latest}
    container_name: ${PLEX_CONTAINER_NAME:-plex}
    restart: ${RESTART:-unless-stopped}

    labels:
      - autoheal-app=true
      - keep_healthy
      - "com.centurylinklabs.watchtower.enable=true"

    volumes:
      - ../DATA/plex/config:/config
      - ../DATA/plex/transcode:/transcode
      - ../DATA/plex/media:/data

    hostname: plex

    # networks:
    #   - media-services
    #   - host

    ports:
      - ${PLEX_PORT:-32400}:32400

    env_file:
      - .env.plex

    deploy:
      replicas: 1
      # resources:
      #   limits:
      #     memory: 2G
      #     cpus: "2.00"
      #   reservations:
      #     memory: 1G
      #     cpus: "1.00"