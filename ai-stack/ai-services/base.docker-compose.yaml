# Shared networks, volumes, and secrets for all services
# NETWORK SEGMENTATION:
# - Use dedicated networks for each stack
# - Restrict inter-stack communication unless required
# - Document network policies
#
# CI/CD & DOCUMENTATION:
# - Use GitHub Actions or similar for automated testing/deployment
# - Document all customizations and manual steps in README.md
# - Keep this file and .env in sync

name: "ai-services"

networks:
  host:
  ai-services:
    external: true

volumes:
  anythingllm_storage:
  anythingllm_hotdir:
  anythingllm_outputs:
  basic-memory-config:
  chroma_data:
  index_data:
  clickhouse_data:
  clickhouse_backups:
  kokoro_data:
  kokoro_index_data:
  faster-whisper-data:
  home-assistant-config:
  letta_pgdata:
  letta_toolexec:
  libretranslate-whispher_data:
  libretranslate-whispher_cache:
  localai_models:
  mongo-whispher_data:
  mongo-whispher_logs:
  n8n_data:
  n8n_local-files:
  ollama-data:
  open-webui-data:
  searxng_data:
  stable-diffusion-webui-docker_data:
  stable-diffusion-webui-docker_config:
  stable-diffusion-webui-docker_models:
  stable-diffusion-webui-docker_output:
  stable-diffusion-webui-docker_embeddings:
  stable-diffusion-webui-docker_workflows:
  swarmdata:
  swarmbackend:
  swarmdlnodes:
  swarmextensions:
  whishper_uploads:
  whishper_logs:
  whishper_models:
  wyoming-piper_data:
# # secrets:
#   letta_pg_password:
#     file: "../../secrets/letta_pg_password"
#   letta_password:
#     file: "../../secrets/letta_password"
#   mariadb_root_password:
#     file: "../../secrets/mariadb_root_password"
#   mariadb_password:
#     file: "../../secrets/mariadb_password"
#   gsdb_root_password:
#     file: "../../secrets/gsdb_root_password"
#   gsdb_password:
#     file: "../../secrets/gsdb_password"
#   romm_db_password:
#     file: "../../secrets/romm_db_password"
#   romm_auth_secret_key:
#     file: "../../secrets/romm_auth_secret_key"
#   screenscraper_password:
#     file: "../../secrets/screenscraper_password"
#   retroachievements_api_key:
#     file: "../../secrets/retroachievements_api_key"
#   steamgriddb_api_key:
#     file: "../../secrets/steamgriddb_api_key"
#   hardcover_auth:
#     file: "../../secrets/hardcover_auth"
#   rreading_glasses_db_password:
#     file: "../../secrets/rreading_glasses_db_password"
